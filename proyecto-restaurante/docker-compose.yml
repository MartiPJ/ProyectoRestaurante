version: "3"
services:
  db:
    image: "postgres:latest"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: restaurante
      POSTGRES_DB: restaurante2025
      POSTGRES_PASSWORD: restaurante